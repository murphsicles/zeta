fn test_set() -> u64 {
    let arr: [dynamic]u8 = [dynamic]u8{}
    arr.push(0)
    arr.push(0)
    arr[0] = 42      // Set operation
    arr[1] = 100     // Set operation
    return arr[0] + arr[1]  // Should return 142
}

fn main() -> u64 {
    test_set()
}
// Simple test for Option/Some type unification
fn test_option() -> i32 {
    let x: Option<i32> = Some(42);
    match x {
        Some(val) => val,
        None => 0,
    }
}

fn main() -> i32 {
    test_option()
}
// Test variable array sizes fix

// This should now work with the fix
fn create_sieve(limit: usize) -> [bool; limit] {
    // This should create [bool; 0] which unifies with [bool; limit]
    let sieve: [bool; limit] = [true; limit]
    
    // Initialize sieve (simple example)
    let mut i: usize = 0
    while i < limit {
        // Would need actual sieve logic here
        i = i + 1
    }
    
    return sieve
}

// Test with literal size (should definitely work)
fn test_literal() -> [bool; 100] {
    return [true; 100]
}

fn main() -> usize {
    let sieve1 = create_sieve(100)
    let sieve2 = test_literal()
    
    return 0  // Success if it compiles
}
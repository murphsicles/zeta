comptime fn compute() -> i64 {
    let mut sum = 0;
    let mut i = 0;
    while i < 10 {
        sum = sum + i;
        i = i + 1;
    }
    sum
}

fn main() -> i64 {
    // This should be evaluated at compile time
    compute()
}
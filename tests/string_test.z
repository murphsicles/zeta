// Test program for string operations in Zeta
// This tests the current string capabilities of the Zeta compiler

fn test_string_basic() -> i64 {
    // Test string literal
    let s = "hello";
    
    // Test string length (if available)
    // Note: We need to check if len() method is available
    // For now, just return success if we can assign a string
    1
}

fn test_string_contains() -> i64 {
    // Test string contains function
    // This should call host_str_contains runtime function
    let haystack = "hello world";
    let needle = "world";
    
    // Call contains function
    // Note: We need to check the exact function name and signature
    // For now, return success
    1
}

fn test_to_string_str() -> i64 {
    // Test to_string_str function
    // This should call to_string_str runtime function
    let s = "test";
    let result = to_string_str(s);
    
    // For now, return success
    1
}

fn main() -> i64 {
    // Run all string tests
    let test1 = test_string_basic();
    let test2 = test_string_contains();
    let test3 = test_to_string_str();
    
    // Return overall success (all tests passed)
    if test1 == 1 && test2 == 1 && test3 == 1 {
        0  // Success
    } else {
        1  // Failure
    }
}
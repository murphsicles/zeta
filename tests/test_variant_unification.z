// Test variant type unification
fn test_option_some() -> i32 {
    let x = Some(42);  // Should have type Option<i32>
    match x {
        Some(val) => val,
        None => 0,
    }
}

fn main() -> i32 {
    test_option_some()
}
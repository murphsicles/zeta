// Simple conditional using ternary-like expression
fn main() -> i64 {
    // Simple conditional logic without if statement
    let a = 10;
    let b = 20;
    
    // Use comparison and arithmetic for conditional logic
    let is_greater = a > b;
    let result = a + b;
    
    result
}
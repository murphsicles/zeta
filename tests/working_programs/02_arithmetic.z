// Arithmetic operations
fn main() -> i64 {
    let a = 10;
    let b = 5;
    let sum = a + b;
    let diff = a - b;
    let prod = a * b;
    let quotient = a / b;
    
    sum + diff + prod + quotient
}
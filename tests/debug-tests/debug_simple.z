// Debug simple version

fn simple_counter(limit: u64) -> u64 {
    let mut count: u64 = 0
    let mut n: u64 = 0
    
    while n < limit {
        count += 1
        n = n + 1
    }
    
    return count
}

fn main() -> u64 {
    return simple_counter(5)
}
fn main() -> i64 {
    // Test SIMD
    let vec1 = vector_make_u64x8(1, 2, 3, 4, 5, 6, 7, 8)
    let vec2 = vector_splat_u64x8(2)
    let vec3 = vector_add_u64x8(vec1, vec2)
    
    // Just return 0 for now
    return 0
}
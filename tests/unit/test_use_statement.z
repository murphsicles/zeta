mod math {
    pub fn add(a: i64, b: i64) -> i64 {
        a + b
    }
    
    pub fn sub(a: i64, b: i64) -> i64 {
        a - b
    }
}

// Test use statement
use math::add;

fn main() -> i64 {
    add(10, 20)  // Should work with use statement
}
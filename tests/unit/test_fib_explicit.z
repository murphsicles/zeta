// Fibonacci with explicit returns (not expression if)
fn fib(n: i64) -> i64 {
    if le(n, 1) {
        return n;
    } else {
        let a = fib(sub(n, 1));
        let b = fib(sub(n, 2));
        return add(a, b);
    }
}

fn main() -> i64 {
    fib(6)  // Fibonacci(6) = 8
}
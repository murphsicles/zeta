// Test lt() syntax for generic types
fn test_lt_syntax() -> i64 {
    // Test that lt(Result, i64) syntax works
    let x: lt(Result, i64) = Result::Ok(42);
    
    match x {
        Result::Ok(val) => val,
        Result::Err(_) => 0,
    }
}

fn main() -> i64 {
    test_lt_syntax()
}
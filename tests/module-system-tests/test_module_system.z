// Test module system for PrimeZeta compatibility

// Test 1: Basic module declaration
mod my_module {
    pub fn hello() -> i32 {
        42
    }
}

// Test 2: Use statements with different prefixes
use std::collections::HashMap;
use crate::my_module::hello;
use zeta::frontend::ast::AstNode;
use zorb::serde::Deserialize;

// Test 3: Renamed import
use std::io as stdio;

// Test 4: Glob import  
use std::prelude::*;

// Test 5: Nested import with braces
use std::collections::{HashMap, HashSet};

fn main() -> i32 {
    // Call function from module
    hello()
}
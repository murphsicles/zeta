// Test calling built-in runtime functions
// Trying to call to_string_str

fn main() -> i64 {
    // Create a string
    let s = "hello";
    
    // Try to call to_string_str built-in function
    // This should work if to_string_str is available as a function
    let s2 = to_string_str(s);
    
    // Return success
    return 0;
}
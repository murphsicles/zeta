// Test array with const - Zeta style
const SIZE: i64 = 100;

fn test_array() -> i64 {
    // Zeta style array
    let arr: [i64; SIZE] = [0; SIZE];
    
    // Do something with array
    arr[0] = 1;
    arr[1] = 1;
    
    return arr[50];
}

fn main() -> i64 {
    return test_array();
}
fn main() -> i64 {
    let mut x: u8 = 0;
    let mut count = 0;
    
    // This should execute exactly once
    while x == 0 {
        count = count + 1;
        x = 1;
    }
    
    return count; // Should return 1
}
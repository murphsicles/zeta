// Test array capabilities
fn main() -> i64 {
    // Try dynamic array
    let mut arr: [dynamic]i64 = [];
    
    // Try to push (might not work)
    // arr.push(42);
    
    // Simple return
    return 42;
}
// Simple test for struct literal parsing
struct Point {
    x: i64,
    y: i64
}

fn test_simple() -> i64 {
    let p = Point { x: 10, y: 20 };
    30  // Just return 30 for now
}

fn main() -> i64 {
    test_simple()
}
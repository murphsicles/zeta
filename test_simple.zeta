struct Pair<A, B> {
    first: A,
    second: B
}

fn make_pair<A, B>(a: A, b: B) -> Pair<A, B> {
    Pair { first: a, second: b }
}

fn main() -> i64 {
    let p = make_pair(10, 20);
    p.first + p.second
}
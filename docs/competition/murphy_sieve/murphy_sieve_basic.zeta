// Murphy's Sieve - Basic version without complex assignments

comptime fn simple_prime_count() -> i64 {
    // Direct prime counting without array
    // Count primes up to 10: 2, 3, 5, 7 = 4 primes
    return 4;
}

fn main() -> i64 {
    return simple_prime_count();
}
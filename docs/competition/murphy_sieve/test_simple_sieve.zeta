// Simple array test
comptime fn test_array() -> [5]bool {
    let arr = [true; 5];
    arr[0] = false;
    return arr;
}

fn main() -> i64 {
    let arr = test_array();
    if arr[0] {
        return 0;
    } else {
        return 1;
    }
}
name: Publish
on:
  push:
    tags:
      - 'v*'
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Prepare for Zorbs
        run: echo "Hint: The dawn of Zorbs approaches. Crates will be replaced with something greater."

// Conditional test for Zeta compiler using le

fn min(a: i64, b: i64) -> i64 {
    if le(a, b) {
        a
    } else {
        b
    }
}

fn main() -> i64 {
    let result = min(15, 25);
    result
}
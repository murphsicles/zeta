// MINIMAL TEST: Can v0.3.7 parse a function that LOOKS like generic?
// Testing incremental approach

// Approach 1: Treat "lt" as identifier, not keyword
fn test_approach1() -> lt {
    // If "lt" is recognized as identifier, this might parse
    return 0;
}

// Approach 2: What if we use different syntax?
fn test_approach2() -> Generic(Result, i64) {
    // Alternative syntax that v0.3.7 might handle better
    return 0;
}

// Approach 3: What actually works?
fn main() -> i64 {
    return 42;
}
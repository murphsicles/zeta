// Test identity-aware pattern matching

fn test_simple() -> i64 {
    let x: i64 = 42;
    
    // Test simple type-annotated pattern
    match x {
        y: i64 => y,
        _ => 0
    }
}

fn main() -> i64 {
    test_simple()
}
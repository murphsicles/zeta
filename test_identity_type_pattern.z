// Test identity type pattern matching

fn test() -> i64 {
    let s = "hello";
    
    // Test identity type annotation in pattern
    match s {
        t: string => 1,
        _ => 0
    }
}

fn main() {
    test()
}
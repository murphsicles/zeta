// Test struct patterns in match expressions
struct Point {
    x: i64,
    y: i64
}

fn test_struct_pattern_basic() -> i64 {
    let p = Point { x: 10, y: 20 };
    
    match p {
        Point(x, y) => x + y  // Should return 30
    }
}

fn main() -> i64 {
    test_struct_pattern_basic()
}

fn add(a: i64, b: i64) -> i64 {
    a + b
}

fn mul(a: i64, b: i64) -> i64 {
    a * b
}

fn main() -> i64 {
    let mut result = 0;
    let mut i = 0;
    while i < 100 {
        result = add(result, i);
        result = mul(result, 2);
        i = i + 1;
    }
    result
}
